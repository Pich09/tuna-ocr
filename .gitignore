__pycache__/
*.pyc
.venv/
*.egg-info/

dataset/
data_gen/samples/
data_gen/generated_pdfs/
data_gen/ocr_dataset/
real_data/samples/

recognizer/tokenizer/assets/
recognizer/checkpoints/

.ipynb_checkpoints/

# One of the HF-token fallback locations the training notebook reads (see its
# section 1). Ignored so a token dropped here can never reach a commit --
# GitHub's push protection rejects that outright and HF auto-revokes the token.
hf_token.txt
